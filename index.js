// Define constant variables
const name = "Susan";
const height = 39;
const message = `${name} is ${height} inches tall`;

// Export variables for testing or usage in other files
module.exports = { name, height, message };
